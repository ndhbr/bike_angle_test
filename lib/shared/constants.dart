import 'package:flutter/material.dart';

/// Round shape for widgets
final RoundedRectangleBorder roundShape = RoundedRectangleBorder(
  borderRadius: BorderRadius.circular(32.0),
);


/// Pagination limits
const LOGBOOK_PAGINATION_LIMIT = 10;